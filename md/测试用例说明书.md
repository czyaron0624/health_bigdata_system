# 健康大数据系统 - 测试用例说明书

**文档版本：** v1.0  
**编写日期：** 2026-04-17  
**适用对象：** 测试人员、开发人员、项目经理  
**项目名称：** health_bigdata_system

---

## 1. 文档说明

### 1.1 编写目的
本文档用于规范健康大数据系统的测试流程，提供完整的测试用例覆盖，确保系统各模块功能正确、数据处理准确、接口响应正常。

### 1.2 测试范围
本文档覆盖以下测试模块：
1. 数据采集模块
2. 数据处理与分析模块
3. Web服务模块
4. 数据库模块
5. 接口集成测试

### 1.3 测试环境
| 环境项 | 配置说明 |
|--------|----------|
| 操作系统 | Windows 11 |
| Python版本 | 3.9+ |
| MySQL版本 | 8.0 |
| Redis版本 | 6.2 |
| Flask版本 | 2.2.5 |
| PySpark版本 | 3.x |
| 测试框架 | pytest / requests |

---

## 2. 测试用例

### 2.1 数据采集模块测试

#### TC-001: 广西卫健委新闻采集测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-001 |
| **测试名称** | 广西卫健委新闻采集功能测试 |
| **测试目的** | 验证广西卫健委爬虫能正确抓取新闻数据并写入数据库 |
| **优先级** | 高 |
| **前置条件** | 1. MySQL服务已启动<br>2. guangxi_news表已创建<br>3. 网络连接正常 |

**测试步骤：**
```python
# 测试脚本：test_guangxi_crawler.py
import mysql.connector
from crawlers.guangxi_health_crawler import main as crawl_guangxi

def test_guangxi_crawl():
    # 1. 执行爬虫
    result = crawl_guangxi()
    
    # 2. 验证爬虫执行成功
    assert result == 0, "爬虫执行失败"
    
    # 3. 验证数据写入
    conn = mysql.connector.connect(
        host="localhost",
        user="root",
        password="rootpassword",
        database="health_db"
    )
    cursor = conn.cursor()
    cursor.execute("SELECT COUNT(*) FROM guangxi_news")
    count = cursor.fetchone()[0]
    
    # 4. 断言数据存在
    assert count > 0, "guangxi_news表无数据"
    
    # 5. 验证必要字段
    cursor.execute("""
        SELECT title, publish_date, link 
        FROM guangxi_news 
        LIMIT 1
    """)
    record = cursor.fetchone()
    assert record[0] is not None, "标题字段为空"
    assert record[1] is not None, "发布日期为空"
    assert record[2] is not None, "链接字段为空"
    
    cursor.close()
    conn.close()
```

**预期结果：**
1. 爬虫执行返回0（成功）
2. guangxi_news表中存在新增记录
3. 记录包含标题、发布日期、链接等必填字段

---

#### TC-002: 国家卫健委新闻采集测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-002 |
| **测试名称** | 国家卫健委新闻采集功能测试 |
| **测试目的** | 验证国家卫健委爬虫能正确抓取新闻数据 |
| **优先级** | 高 |
| **前置条件** | 1. MySQL服务已启动<br>2. national_news表已创建 |

**测试步骤：**
```python
# 测试脚本：test_national_crawler.py
import mysql.connector
from crawlers.national_health_crawler import main as crawl_national

def test_national_crawl():
    # 1. 执行爬虫
    result = crawl_national()
    
    # 2. 验证执行结果
    assert result == 0, "国家卫健委爬虫执行失败"
    
    # 3. 验证数据
    conn = mysql.connector.connect(
        host="localhost",
        user="root",
        password="rootpassword",
        database="health_db"
    )
    cursor = conn.cursor()
    cursor.execute("SELECT COUNT(*) FROM national_news")
    count = cursor.fetchone()[0]
    assert count > 0, "national_news表无数据"
    
    # 4. 验证数据完整性
    cursor.execute("""
        SELECT COUNT(*) FROM national_news 
        WHERE title IS NULL OR content IS NULL
    """)
    null_count = cursor.fetchone()[0]
    assert null_count == 0, f"存在{null_count}条空数据记录"
    
    cursor.close()
    conn.close()
```

**预期结果：**
1. 爬虫执行成功
2. national_news表有数据写入
3. 无空值记录

---

#### TC-003: 深度爬虫测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-003 |
| **测试名称** | 深度爬虫功能测试 |
| **测试目的** | 验证deep_crawler能正确抓取详情页内容 |
| **优先级** | 中 |
| **前置条件** | 1. 基础爬虫已运行<br>2. 存在待深度抓取的链接 |

**测试步骤：**
```python
def test_deep_crawl():
    from crawlers.deep_crawler import main as deep_crawl
    
    # 执行深度爬虫
    result = deep_crawl()
    assert result == 0, "深度爬虫执行失败"
    
    # 验证内容已更新
    conn = mysql.connector.connect(...)
    cursor = conn.cursor()
    cursor.execute("""
        SELECT COUNT(*) FROM guangxi_news 
        WHERE content IS NOT NULL AND LENGTH(content) > 100
    """)
    rich_content_count = cursor.fetchone()[0]
    assert rich_content_count > 0, "无丰富内容记录"
```

**预期结果：**
1. 深度爬虫执行成功
2. 新闻内容被补充完整

---

### 2. 数据处理与分析模块测试

#### TC-004: OCR结构化指标抽取测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-004 |
| **测试名称** | OCR结构化指标抽取测试 |
| **测试目的** | 验证OCR能从新闻文本中提取结构化指标 |
| **优先级** | 高 |
| **前置条件** | 1. 新闻数据已采集<br>2. health_ocr_metrics表已创建 |

**测试步骤：**
```python
def test_ocr_extraction():
    from crawlers.backfill_ocr_metrics import main as backfill
    
    # 1. 执行OCR回填
    result = backfill()
    assert result == 0, "OCR回填执行失败"
    
    # 2. 验证指标表数据
    conn = mysql.connector.connect(...)
    cursor = conn.cursor(dictionary=True)
    
    cursor.execute("SELECT COUNT(*) AS cnt FROM health_ocr_metrics")
    count = cursor.fetchone()["cnt"]
    assert count > 0, "health_ocr_metrics表无数据"
    
    # 3. 验证指标键完整性
    cursor.execute("""
        SELECT DISTINCT metric_key FROM health_ocr_metrics
    """)
    keys = [row["metric_key"] for row in cursor.fetchall()]
    
    expected_keys = [
        "doctor_count", "nurse_count", "bed_count",
        "outpatient_visits", "discharge_count"
    ]
    for key in expected_keys:
        assert key in keys, f"缺少指标键: {key}"
    
    # 4. 验证指标值合理性
    cursor.execute("""
        SELECT metric_key, metric_value 
        FROM health_ocr_metrics 
        WHERE metric_value <= 0
    """)
    invalid_values = cursor.fetchall()
    assert len(invalid_values) == 0, f"存在{len(invalid_values)}个无效指标值"
    
    cursor.close()
    conn.close()
```

**预期结果：**
1. OCR回填执行成功
2. health_ocr_metrics表有数据
3. 包含所有必需的指标键
4. 指标值均为正数

---

#### TC-005: 六大模块处理测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-005 |
| **测试名称** | 六大模块处理功能测试 |
| **测试目的** | 验证六大模块分析结果表生成正确 |
| **优先级** | 高 |
| **前置条件** | 1. 指标数据已准备<br>2. 视图已创建 |

**测试步骤：**
```python
def test_six_modules():
    from spark_job.six_modules_processor import main as process_modules
    
    # 1. 执行六大模块处理
    result = process_modules()
    assert result == 0, "六大模块处理失败"
    
    # 2. 验证结果表
    conn = mysql.connector.connect(...)
    cursor = conn.cursor()
    
    result_tables = [
        "analysis_population_region",
        "analysis_population_age",
        "analysis_institution_type",
        "analysis_personnel",
        "analysis_beds",
        "analysis_services",
        "analysis_costs"
    ]
    
    for table in result_tables:
        cursor.execute(f"SELECT COUNT(*) FROM {table}")
        count = cursor.fetchone()[0]
        assert count > 0, f"{table}表无数据"
    
    cursor.close()
    conn.close()
```

**预期结果：**
1. 六大模块处理执行成功
2. 所有分析结果表都有数据

---

#### TC-006: 年度汇总处理测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-006 |
| **测试名称** | 年度汇总处理功能测试 |
| **测试目的** | 验证ocr_metrics_yearly表生成正确 |
| **优先级** | 高 |
| **前置条件** | health_ocr_metrics表有数据 |

**测试步骤：**
```python
def test_yearly_summary():
    from spark_job.yearly_metrics_processor import main as yearly_process
    
    # 执行年度汇总
    result = yearly_process()
    assert result == 0, "年度汇总处理失败"
    
    # 验证汇总表
    conn = mysql.connector.connect(...)
    cursor = conn.cursor(dictionary=True)
    
    cursor.execute("SELECT COUNT(*) AS cnt FROM ocr_metrics_yearly")
    count = cursor.fetchone()["cnt"]
    assert count > 0, "ocr_metrics_yearly表无数据"
    
    # 验证必要字段
    cursor.execute("""
        SELECT region, year, doctor_count, nurse_count, bed_count 
        FROM ocr_metrics_yearly 
        LIMIT 1
    """)
    record = cursor.fetchone()
    assert record["region"] is not None, "地区字段为空"
    assert record["year"] is not None, "年份字段为空"
    
    cursor.close()
    conn.close()
```

**预期结果：**
1. 年度汇总处理成功
2. ocr_metrics_yearly表有按地区按年份的汇总数据

---

#### TC-007: 区域对比分析测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-007 |
| **测试名称** | 区域对比分析功能测试 |
| **测试目的** | 验证region_comparison表计算正确 |
| **优先级** | 高 |
| **前置条件** | 1. ocr_metrics_yearly有数据<br>2. population_info有数据 |

**测试步骤：**
```python
def test_region_comparison():
    from spark_job.region_comparison_analyzer import main as analyze_region
    
    # 执行区域对比
    result = analyze_region()
    assert result == 0, "区域对比分析失败"
    
    # 验证对比表
    conn = mysql.connector.connect(...)
    cursor = conn.cursor(dictionary=True)
    
    cursor.execute("SELECT COUNT(*) AS cnt FROM region_comparison")
    count = cursor.fetchone()["cnt"]
    assert count > 0, "region_comparison表无数据"
    
    # 验证评分字段
    cursor.execute("""
        SELECT resource_score, service_score 
        FROM region_comparison 
        WHERE resource_score IS NOT NULL
    """)
    records = cursor.fetchall()
    for record in records:
        assert 0 <= record["resource_score"] <= 100, "资源评分超出范围"
        assert 0 <= record["service_score"] <= 100, "服务评分超出范围"
    
    cursor.close()
    conn.close()
```

**预期结果：**
1. 区域对比分析成功
2. 评分字段在合理范围内

---

#### TC-008: 趋势预测测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-008 |
| **测试名称** | 趋势预测功能测试 |
| **测试目的** | 验证prediction_results表生成正确 |
| **优先级** | 高 |
| **前置条件** | ocr_metrics_yearly有足够历史数据 |

**测试步骤：**
```python
def test_trend_prediction():
    from spark_job.trend_prediction import main as predict
    
    # 执行趋势预测
    result = predict()
    assert result == 0, "趋势预测执行失败"
    
    # 验证预测表
    conn = mysql.connector.connect(...)
    cursor = conn.cursor(dictionary=True)
    
    cursor.execute("SELECT COUNT(*) AS cnt FROM prediction_results")
    count = cursor.fetchone()["cnt"]
    assert count > 0, "prediction_results表无数据"
    
    # 验证置信区间
    cursor.execute("""
        SELECT predict_value, confidence_lower, confidence_upper 
        FROM prediction_results
    """)
    records = cursor.fetchall()
    for record in records:
        assert record["confidence_lower"] <= record["predict_value"], "置信下限大于预测值"
        assert record["predict_value"] <= record["confidence_upper"], "预测值大于置信上限"
    
    cursor.close()
    conn.close()
```

**预期结果：**
1. 趋势预测执行成功
2. 预测值在置信区间内

---

#### TC-009: 异常检测测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-009 |
| **测试名称** | 异常检测功能测试 |
| **测试目的** | 验证anomaly_detection表生成正确 |
| **优先级** | 高 |
| **前置条件** | 1. ocr_metrics_yearly有数据<br>2. prediction_results有数据 |

**测试步骤：**
```python
def test_anomaly_detection():
    from spark_job.anomaly_detection import main as detect_anomaly
    
    # 执行异常检测
    result = detect_anomaly()
    assert result == 0, "异常检测执行失败"
    
    # 验证预警表
    conn = mysql.connector.connect(...)
    cursor = conn.cursor(dictionary=True)
    
    cursor.execute("SELECT COUNT(*) AS cnt FROM anomaly_detection")
    count = cursor.fetchone()["cnt"]
    assert count > 0, "anomaly_detection表无数据"
    
    # 验证告警级别
    cursor.execute("""
        SELECT DISTINCT anomaly_level FROM anomaly_detection
    """)
    levels = [row["anomaly_level"] for row in cursor.fetchall()]
    valid_levels = ["normal", "warning", "critical"]
    for level in levels:
        assert level in valid_levels, f"无效告警级别: {level}"
    
    # 验证偏离率计算
    cursor.execute("""
        SELECT actual_value, expected_value, deviation_rate 
        FROM anomaly_detection 
        WHERE expected_value > 0
    """)
    records = cursor.fetchall()
    for record in records:
        expected_rate = abs(record["actual_value"] - record["expected_value"]) / record["expected_value"]
        assert abs(record["deviation_rate"] - expected_rate) < 0.01, "偏离率计算错误"
    
    cursor.close()
    conn.close()
```

**预期结果：**
1. 异常检测执行成功
2. 告警级别仅为normal/warning/critical
3. 偏离率计算正确

---

### 2.3 Web服务模块测试

#### TC-010: 用户登录测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-010 |
| **测试名称** | 用户登录功能测试 |
| **测试目的** | 验证管理员和用户登录功能正常 |
| **优先级** | 高 |
| **前置条件** | Flask服务已启动 |

**测试步骤：**
```python
def test_user_login():
    import requests
    
    base_url = "http://127.0.0.1:5000"
    session = requests.Session()
    
    # 1. 测试管理员登录
    admin_resp = session.post(
        f"{base_url}/login",
        data={
            "username": "admin",
            "password": "admin123",
            "role": "admin"
        },
        allow_redirects=False
    )
    assert admin_resp.status_code in [200, 301, 302], "管理员登录失败"
    
    # 2. 验证重定向到管理端
    if admin_resp.status_code in [301, 302]:
        location = admin_resp.headers.get("Location", "")
        assert "/admin/dashboard" in location, "未重定向到管理端"
    
    # 3. 测试用户登录
    session2 = requests.Session()
    user_resp = session2.post(
        f"{base_url}/login",
        data={
            "username": "user",
            "password": "user123",
            "role": "user"
        },
        allow_redirects=False
    )
    assert user_resp.status_code in [200, 301, 302], "用户登录失败"
```

**预期结果：**
1. 管理员登录成功，重定向到/admin/dashboard
2. 用户登录成功，重定向到/user/dashboard

---

#### TC-011: API接口连通性测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-011 |
| **测试名称** | API接口连通性测试 |
| **测试目的** | 验证各API接口响应正常 |
| **优先级** | 高 |
| **前置条件** | 1. Flask服务已启动<br>2. 已登录获取Session |

**测试步骤：**
```python
def test_api_endpoints():
    import requests
    
    base_url = "http://127.0.0.1:5000"
    session = requests.Session()
    
    # 先登录
    session.post(f"{base_url}/login", data={
        "username": "admin",
        "password": "admin123",
        "role": "admin"
    })
    
    # 测试各API端点
    endpoints = [
        ("/api/health-stats", "健康统计"),
        ("/api/metrics/summary", "指标汇总"),
        ("/api/news/national", "国家新闻"),
        ("/api/news/guangxi", "广西新闻"),
        ("/api/news/region?scope=guangxi", "区域新闻"),
        ("/api/analysis/population", "人口分析"),
        ("/api/analysis/institutions", "机构分析"),
        ("/api/analysis/personnel", "人员分析"),
        ("/api/analysis/beds", "床位分析"),
        ("/api/analysis/services", "服务分析"),
        ("/api/analysis/costs", "费用分析"),
        ("/api/metrics/yearly", "年度指标"),
        ("/api/analysis/region-comparison", "区域对比"),
        ("/api/prediction/results", "预测结果"),
        ("/api/anomaly/alerts", "异常预警"),
    ]
    
    for path, name in endpoints:
        resp = session.get(f"{base_url}{path}", timeout=12)
        assert resp.status_code == 200, f"{name}接口返回{resp.status_code}"
        
        # 验证JSON响应
        data = resp.json()
        assert data is not None, f"{name}接口返回空数据"
```

**预期结果：**
1. 所有API端点返回200状态码
2. 响应内容为有效JSON

---

#### TC-012: 文档上传预览测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-012 |
| **测试名称** | 文档上传预览功能测试 |
| **测试目的** | 验证文件上传和预览功能正常 |
| **优先级** | 高 |
| **前置条件** | 1. Flask服务已启动<br>2. 准备测试文件 |

**测试步骤：**
```python
def test_document_upload_preview():
    import requests
    
    base_url = "http://127.0.0.1:5000"
    session = requests.Session()
    
    # 登录
    session.post(f"{base_url}/login", data={
        "username": "admin",
        "password": "admin123",
        "role": "admin"
    })
    
    # 准备测试文件
    test_file_path = "inputs/广西省十年卫生机构.xlsx"
    
    with open(test_file_path, "rb") as f:
        files = {"file": ("test.xlsx", f, "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")}
        resp = session.post(f"{base_url}/upload/preview", files=files)
    
    assert resp.status_code == 200, "预览请求失败"
    
    data = resp.json()
    assert data.get("success") == True, "预览失败"
    assert "preview" in data or "data" in data, "响应缺少预览数据"
```

**预期结果：**
1. 上传请求成功
2. 返回预览数据

---

#### TC-013: 文档确认入库测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-013 |
| **测试名称** | 文档确认入库功能测试 |
| **测试目的** | 验证预览数据能正确写入数据库 |
| **优先级** | 高 |
| **前置条件** | 1. 已完成预览测试<br>2. 目标表已存在 |

**测试步骤：**
```python
def test_document_confirm():
    import requests
    import mysql.connector
    
    base_url = "http://127.0.0.1:5000"
    session = requests.Session()
    
    # 登录
    session.post(f"{base_url}/login", data={
        "username": "admin",
        "password": "admin123",
        "role": "admin"
    })
    
    # 获取当前记录数
    conn = mysql.connector.connect(...)
    cursor = conn.cursor()
    cursor.execute("SELECT COUNT(*) FROM medical_institution")
    before_count = cursor.fetchone()[0]
    
    # 执行确认入库
    confirm_data = {
        "target_table": "medical_institution",
        "filename": "test_upload.xlsx"
    }
    resp = session.post(f"{base_url}/upload/confirm", json=confirm_data)
    
    assert resp.status_code == 200, "确认入库请求失败"
    
    # 验证数据写入
    cursor.execute("SELECT COUNT(*) FROM medical_institution")
    after_count = cursor.fetchone()[0]
    assert after_count > before_count, "数据未写入"
    
    cursor.close()
    conn.close()
```

**预期结果：**
1. 确认入库请求成功
2. 数据库记录数增加

---

### 2.4 数据库模块测试

#### TC-014: 数据库表结构验证测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-014 |
| **测试名称** | 数据库表结构验证测试 |
| **测试目的** | 验证所有必需表和视图存在 |
| **优先级** | 高 |
| **前置条件** | MySQL服务已启动 |

**测试步骤：**
```python
def test_table_structure():
    import mysql.connector
    
    conn = mysql.connector.connect(
        host="localhost",
        user="root",
        password="rootpassword",
        database="health_db"
    )
    cursor = conn.cursor()
    
    # 验证核心表
    required_tables = [
        "guangxi_news",
        "national_news",
        "sichuan_news",
        "health_ocr_metrics",
        "population_info",
        "medical_institution",
        "ocr_metrics_yearly",
        "region_comparison",
        "prediction_results",
        "anomaly_detection",
        "analysis_population_region",
        "analysis_institution_type",
        "analysis_personnel",
        "analysis_beds",
        "analysis_services",
        "analysis_costs"
    ]
    
    for table in required_tables:
        cursor.execute(f"""
            SELECT COUNT(*) FROM information_schema.tables 
            WHERE table_schema = DATABASE() AND table_name = '{table}'
        """)
        exists = cursor.fetchone()[0]
        assert exists > 0, f"表{table}不存在"
    
    # 验证视图
    required_views = [
        "vw_metric_clean",
        "v_population_stats",
        "v_institution_stats",
        "v_personnel_stats"
    ]
    
    for view in required_views:
        cursor.execute(f"""
            SELECT COUNT(*) FROM information_schema.views 
            WHERE table_schema = DATABASE() AND table_name = '{view}'
        """)
        exists = cursor.fetchone()[0]
        assert exists > 0, f"视图{view}不存在"
    
    cursor.close()
    conn.close()
```

**预期结果：**
1. 所有必需表存在
2. 所有必需视图存在

---

#### TC-015: 数据完整性测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-015 |
| **测试名称** | 数据完整性验证测试 |
| **测试目的** | 验证核心数据表的数据完整性 |
| **优先级** | 中 |
| **前置条件** | 数据已导入 |

**测试步骤：**
```python
def test_data_integrity():
    import mysql.connector
    
    conn = mysql.connector.connect(...)
    cursor = conn.cursor(dictionary=True)
    
    # 1. 验证health_ocr_metrics必填字段
    cursor.execute("""
        SELECT COUNT(*) AS cnt FROM health_ocr_metrics 
        WHERE metric_key IS NULL OR metric_value IS NULL
    """)
    null_count = cursor.fetchone()["cnt"]
    assert null_count == 0, f"health_ocr_metrics存在{null_count}条空值记录"
    
    # 2. 验证年份范围合理性
    cursor.execute("""
        SELECT COUNT(*) AS cnt FROM health_ocr_metrics 
        WHERE year < 2000 OR year > 2030
    """)
    invalid_year = cursor.fetchone()["cnt"]
    assert invalid_year == 0, f"存在{invalid_year}条年份异常记录"
    
    # 3. 验证指标值为正数
    cursor.execute("""
        SELECT COUNT(*) AS cnt FROM health_ocr_metrics 
        WHERE metric_value < 0
    """)
    negative_value = cursor.fetchone()["cnt"]
    assert negative_value == 0, f"存在{negative_value}条负值记录"
    
    # 4. 验证外键关联
    cursor.execute("""
        SELECT COUNT(*) AS cnt FROM health_ocr_metrics h
        LEFT JOIN guangxi_news g ON h.news_id = g.id
        WHERE h.source_table = 'guangxi_news' AND g.id IS NULL
    """)
    orphan_records = cursor.fetchone()["cnt"]
    assert orphan_records == 0, f"存在{orphan_records}条孤立记录"
    
    cursor.close()
    conn.close()
```

**预期结果：**
1. 无空值必填字段
2. 年份在合理范围内
3. 指标值为非负数
4. 外键关联正确

---

### 2.5 集成测试

#### TC-016: 端到端数据流测试

| 属性 | 说明 |
|------|------|
| **测试编号** | TC-016 |
| **测试名称** | 端到端数据流集成测试 |
| **测试目的** | 验证从采集到展示的完整数据流 |
| **优先级** | 高 |
| **前置条件** | 系统各组件已部署 |

**测试步骤：**
```python
def test_end_to_end_flow():
    """测试完整的数据处理流程"""
    
    # 1. 执行数据采集
    from crawlers.guangxi_health_crawler import main as crawl
    assert crawl() == 0, "数据采集失败"
    
    # 2. 执行OCR结构化
    from crawlers.backfill_ocr_metrics import main as ocr
    assert ocr() == 0, "OCR结构化失败"
    
    # 3. 执行年度汇总
    from spark_job.yearly_metrics_processor import main as yearly
    assert yearly() == 0, "年度汇总失败"
    
    # 4. 执行区域对比
    from spark_job.region_comparison_analyzer import main as region
    assert region() == 0, "区域对比失败"
    
    # 5. 执行趋势预测
    from spark_job.trend_prediction import main as predict
    assert predict() == 0, "趋势预测失败"
    
    # 6. 执行异常检测
    from spark_job.anomaly_detection import main as anomaly
    assert anomaly() == 0, "异常检测失败"
    
    # 7. 验证API可读取
    import requests
    session = requests.Session()
    session.post("http://127.0.0.1:5000/login", data={
        "username": "admin",
        "password": "admin123",
        "role": "admin"
    })
    
    resp = session.get("http://127.0.0.1:5000/api/prediction/results")
    assert resp.status_code == 200, "预测结果API不可用"
    
    data = resp.json()
    assert data.get("success") == True, "预测结果获取失败"
```

**预期结果：**
1. 所有步骤执行成功
2. API能正确返回处理结果

---

## 3. 测试执行指南

### 3.1 测试环境准备

**步骤1：启动基础服务**
```bash
# 启动Docker容器
docker-compose up -d

# 等待MySQL启动
sleep 10
```

**步骤2：初始化数据库**
```bash
# 执行迁移脚本
python -m flask db upgrade

# 或手动执行SQL
mysql -u root -prootpassword health_db < migrations/create_analysis_views.sql
```

**步骤3：准备测试数据**
```bash
# 导入基础数据
python tools/import_guangxi_population.py
python tools/import_guangxi_institution_yearly.py
```

### 3.2 测试执行顺序

**推荐执行顺序：**
1. 数据库模块测试 (TC-014, TC-015)
2. 数据采集模块测试 (TC-001, TC-002, TC-003)
3. 数据处理模块测试 (TC-004 ~ TC-009)
4. Web服务模块测试 (TC-010 ~ TC-013)
5. 集成测试 (TC-016)

### 3.3 测试命令

**运行单个测试：**
```bash
pytest tests/test_guangxi_crawler.py -v
```

**运行全部测试：**
```bash
pytest tests/ -v
```

**生成测试报告：**
```bash
pytest tests/ -v --html=reports/test_report.html
```

---

## 4. 测试数据准备

### 4.1 测试数据清单

| 数据类型 | 文件路径 | 用途 |
|----------|----------|------|
| 人口数据 | inputs/广西常住人口年龄构成（2014-2023年）.xlsx | 人口分析测试 |
| 机构数据 | inputs/广西省十年卫生机构.xlsx | 机构分析测试 |
| 测试CSV | outputs/health_structured_*.csv | 结果验证 |

### 4.2 Mock数据生成

```python
# 生成测试用新闻数据
def generate_test_news(count=10):
    news_list = []
    for i in range(count):
        news_list.append({
            "title": f"测试新闻{i}",
            "content": f"医生数量{i * 100}人，护士数量{i * 150}人",
            "publish_date": f"2024-0{i % 9 + 1}-15",
            "link": f"http://test.com/news/{i}"
        })
    return news_list
```

---

## 5. 测试验收标准

### 5.1 功能测试验收标准
- 所有测试用例执行通过率 ≥ 95%
- 严重级别(Bug)数量 = 0
- 高优先级测试用例通过率 = 100%

### 5.2 性能测试验收标准
- API响应时间 < 3秒
- 数据处理任务执行时间 < 10分钟
- 页面加载时间 < 5秒

### 5.3 数据质量验收标准
- 数据完整性 ≥ 99%
- 数据准确性 ≥ 98%
- 无重复记录

---

## 6. 缺陷管理

### 6.1 缺陷严重级别

| 级别 | 定义 | 处理时限 |
|------|------|----------|
| 严重 | 系统崩溃、数据丢失 | 立即处理 |
| 高 | 核心功能不可用 | 24小时内 |
| 中 | 功能异常但有替代方案 | 3天内 |
| 低 | 界面问题、体验优化 | 下个版本 |

### 6.2 缺陷报告模板

```markdown
**缺陷标题：** [模块] 简要描述
**测试用例：** TC-XXX
**严重级别：** 严重/高/中/低
**重现步骤：**
1. 步骤1
2. 步骤2
3. ...

**期望结果：** 
**实际结果：**
**环境信息：** 
**附件：** 截图/日志
```

---

## 7. 附录

### 7.1 现有测试脚本清单

| 脚本路径 | 功能说明 |
|----------|----------|
| tools/checks/test_api.py | API连通性测试 |
| tools/checks/verify_data.py | 数据验证 |
| tools/checks/check_category.py | 分类检查 |
| tools/checks/test_nhc_access.py | 国家卫健委访问测试 |
| spark_job/spark_test.py | Spark功能测试 |

### 7.2 测试工具清单

| 工具 | 用途 |
|------|------|
| pytest | Python单元测试框架 |
| requests | HTTP接口测试 |
| mysql.connector | 数据库测试 |
| selenium | UI自动化测试（可选） |

---

**文档结束**